# PostgreSQL service for Kubernetes cluster
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: econ-graph
  labels:
    app: postgresql
spec:
  type: ClusterIP
  selector:
    app: postgresql
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
