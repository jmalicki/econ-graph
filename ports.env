# Port Configuration for EconGraph Development
# This file contains non-secret configuration that can be shared between frontend and backend

# Backend API Server Port (internal container port)
BACKEND_PORT=9876

# Frontend Development Server Port (internal container port)
FRONTEND_PORT=3000

# Admin Frontend Development Server Port
ADMIN_FRONTEND_PORT=3001

# Database Port (for reference)
DATABASE_PORT=5432

# Redis Port (if used in future)
REDIS_PORT=6379

# Kubernetes NodePort Mappings (external access)
BACKEND_NODEPORT=30080
FRONTEND_NODEPORT=30000
GRAFANA_NODEPORT=30001

# Service URLs for external access
BACKEND_URL=http://localhost:${BACKEND_NODEPORT}
FRONTEND_URL=http://localhost:${FRONTEND_NODEPORT}
GRAFANA_URL=http://localhost:${GRAFANA_NODEPORT}
